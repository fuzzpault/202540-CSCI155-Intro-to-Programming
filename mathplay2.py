a = float(input("First number"))
b = float(input("Second number"))

print( b % a) # Modulo operator.  The remainder after division
# anything % 5 -> 0, 1, 2, 3, 4 Output must be in the range 0... (mod number - 1)

print( b ** a)

# How can I convert a number into a string?  (a+b)
str1 = f"{a + b}"  #(a +b)
str2 = " nothing"
print(str1 + str2)